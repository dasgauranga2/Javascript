
// declaring boolean variables
const has_license = true;
const has_vision = false;

// logical AND oparator
console.log(has_license && has_vision);
// logical OR oparator
console.log(has_license || has_vision);
// logical NOT oparator
console.log(!has_license);