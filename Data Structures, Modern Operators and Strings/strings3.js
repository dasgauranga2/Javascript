'use strict';

const message = 'a.very.nice.string';
// split the string into at occurence of the specified substring
console.log(message.split('.'));

const parts = ['Mr.','Gauranga','Das'];
// join the elements of array of strings with the
// sepcified substring in between the elements
console.log(parts.join(' '));